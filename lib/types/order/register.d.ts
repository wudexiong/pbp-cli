import { contextType } from '../index';
declare const _default: {
    naem: string;
    /**
     * @description: 注册命令
     * @param {string} contextType
     * @return {void}
     */
    apply({ params }: contextType): void;
};
export default _default;
